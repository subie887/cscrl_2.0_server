generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Videos {
  id        String @id @default(auto()) @map("_id") @db.ObjectId
  fileName  String
  eventName String
  title     String
  desc      String
}
